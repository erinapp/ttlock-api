export interface ParameterInterface {
    clientId: string;
    clientSecret: string;
    username: string;
    password: string;
    date: string;
}

export interface ResponseInterface {
    errcode: number;
    errmsg: string;
}